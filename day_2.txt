Day-2
======================
1.进行数字大小对比时，一定要注意两边是否均为有符号或无符号

2.在进行除法时，若结果要输出小数，可以强制改变赋值为浮点数，就是在其前加上（float）
如：
float result_float = (float)x/y;
printf("【实验二】强制转换后：(float)%d / %d = %f(保留小数)\n",x,y,result_float);

3.char的存储为128位
  unsigned char的存储上限为256位